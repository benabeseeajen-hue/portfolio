<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect everything to the /public folder (simulates setting the docroot to public).
    # This keeps the full Laravel directory structure intact on InfinityFree.
    RewriteRule (.*) /public/$1 [L]
</IfModule>
